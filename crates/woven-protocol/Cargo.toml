[package]
name = "woven-protocol"
version = "0.2.0"
edition.workspace = true
rust-version.workspace = true
description = "Transport-neutral FlatBuffers protocol and bounded framing for Woven"
license.workspace = true
repository.workspace = true
build = "build.rs"

[lib]
path = "src/lib.rs"

[dependencies]
flatbuffers.workspace = true

[build-dependencies]
flatbuffers-build.workspace = true
flatc-fork.workspace = true

[lints]
workspace = true
